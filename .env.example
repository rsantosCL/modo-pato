POSTGRES_DB=modopato
POSTGRES_USER=modopato
POSTGRES_PASSWORD=changeme

DATABASE_URL=postgres://modopato:changeme@db:5432/modopato

SECRET_KEY=change-this-to-a-random-secret-key
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,backend,admin
CORS_ALLOWED_ORIGINS=http://localhost:5173

# URL the browser uses to reach the API (not the docker-internal hostname).
VITE_API_BASE_URL=http://localhost:8000
